# Optional production CORS (comma-separated). Always include your real site origin.
# CORS_ORIGINS=https://your-portfolio.vercel.app

# Optional: allow every Vercel preview deployment (regex). Use with care.
# CORS_ORIGIN_REGEX=https://.*\.vercel\.app

# Host platforms (Railway, Render, Fly) set PORT automatically when using Docker.
